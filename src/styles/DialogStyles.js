import styled from "styled-components";

const StyledDialog = styled.dialog`
  border-radius: 10px;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  margin: auto;
  position: absolute;
`;
export { StyledDialog };
